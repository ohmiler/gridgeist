I can fix this once you provide the file or component containing the Save button and its current click/submit handler. No target fixture or source was supplied, so changing the repository now would require guessing.

The change will be limited to ensuring one submission per click while preserving the current markup, styling, and UI behavior—no redesign.
