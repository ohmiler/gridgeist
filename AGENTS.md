# Project instructions

- Do not use any `superpowers:*` skills in this repository.
- Do not create or restore files under `docs/superpowers/`.
- Use the `solodeveling` skill at the start of every task in this repository. Resume `.solodeveling/state.md` when present and let the skill route the work to the smallest relevant lifecycle workflow.
- Keep one primary agent. Do not use subagents unless the user explicitly requests delegation.
- If `solodeveling` is unavailable, state that briefly and continue with the same evidence-driven lifecycle rather than blocking routine work.
- Work directly from the user's request and use only the minimum additional relevant skills.
